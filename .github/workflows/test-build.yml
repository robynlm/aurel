name: Test build

on:
  push:
    branches:
      - development
      - main

permissions:
  contents: write  # Allow pushing coverage badge updates

jobs:
  test-and-update:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ['3.11', '3.12', '3.13', '3.14']

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        pip install -r requirements.txt
        pip install build twine
        pip install -r docs/requirements.txt
        pip install .[test]

    - name: Build the package
      run: python -m build
    
    - name: Run the tests with coverage
      run: pytest --cov=aurel --cov-report=term --cov-report=json
    
    - name: Generate coverage badge data
      if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.12'
      run: |
        COVERAGE=$(python -c "import json; print(int(json.load(open('coverage.json'))['totals']['percent_covered']))")
        COLOR="brightgreen"
        if [ $COVERAGE -lt 80 ]; then COLOR="yellow"; fi
        if [ $COVERAGE -lt 60 ]; then COLOR="orange"; fi
        if [ $COVERAGE -lt 40 ]; then COLOR="red"; fi
        mkdir -p .github/badges
        echo "{\"schemaVersion\": 1, \"label\": \"coverage\", \"message\": \"${COVERAGE}%\", \"color\": \"${COLOR}\"}" > .github/badges/coverage.json
        
    - name: Commit coverage badge data
      if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.12'
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add .github/badges/coverage.json
        git diff --quiet && git diff --staged --quiet || git commit -m "Update coverage badge data"
        git push
    
    - name: Build documentation
      run: |
        cd docs
        make html